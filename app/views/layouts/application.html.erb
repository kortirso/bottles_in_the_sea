<!DOCTYPE html>
<html>
  <head>
    <title>Bottles in the Sea</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
  </head>
  <body>
    <input type="hidden" id="current_locale" value="<%= I18n.locale %>" />
    <%= yield %>
    <%= javascript_include_tag 'application' %>
  </body>
</html>
